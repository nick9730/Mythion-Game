// Fill out your copyright notice in the Description page of Project Settings.


#include "M_QuestController.h"
#include "AM_QuestNPC.h"
#include "M_QuestStatus.h"

void AM_QuestController::OnPossess(APawn* InPawn)
{
    /*
	Super::OnPossess(InPawn);
    AAM_QuestNPC* NPC = Cast<AAM_QuestNPC>(InPawn);

    if (!IsValid(NPC)) return;

    if (!IsValid(NPC->QuestStatusUserWidget)) return;
    NPC->QuestStatusUserWidget->UpdateQuestStatusImage(NPC->CurrentQuestData);
    */

}
